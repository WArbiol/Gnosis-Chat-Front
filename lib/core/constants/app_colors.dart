import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — warm gold (gnostic/esoteric tone)
  static const Color primary = Color(0xFFD4A537);
  static const Color primaryDark = Color(0xFFB8922F);
  static const Color primaryLight = Color(0xFFE8C561);

  // Neutral
  static const Color background = Color(0xFF0F0F14);
  static const Color surface = Color(0xFF1A1A24);
  static const Color surfaceVariant = Color(0xFF252533);
  static const Color onSurface = Color(0xFFE8E6E3);
  static const Color onSurfaceVariant = Color(0xFF9E9E9E);

  // Accent
  static const Color accent = Color(0xFF5BA5A5);

  // Semantic
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
}
